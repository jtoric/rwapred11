# URL backend API-ja. Čita se pri build-u (VITE_ prefix = dostupno u browseru).
# Dev:  automatski čita .env.development (npm run dev)
# Prod: automatski čita .env.production (npm run build)
VITE_API_URL=http://localhost:8000
